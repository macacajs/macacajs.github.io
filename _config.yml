name: Macaca

baseurl: ""

collections:
  docs:
    output: true
    permalink: /:path.html

defaults:
  -
    scope:
      path: "_docs"
    values:
      layout: docs

timezone: Asia/Shanghai

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - LICENSE
  - package.json
  - node_modules
  - npm-debug.log

sass:
  style: compressed

plugins:
  - jekyll-multiple-languages-plugin

languages: ["en", "zh"]

exclude_from_localizations: ["assets", "redirects"]
