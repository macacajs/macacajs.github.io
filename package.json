{
  "name": "macacajs.github.io",
  "devDependencies": {
    "gh-pages": "^0.12.0"
  },
  "scripts": {
    "server": "jekyll server",
    "build": "jekyll build",
    "update": "jekyll build && gh-pages -d _site -b master"
  }
}
